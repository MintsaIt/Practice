//비밀번호 입력 프로그램

#include <stdio.h>
#include <string.h>

void main()
{
	char password[S] = "5678";
	char input[5];
	int i;

	printf("비밀번호 4글자를 입력하세요. : ");
	for(i=0; i<4; i++)
		input[i] = 

		if (stencmp(password, input, 4) == 0)
		{
			printf("\n비밀번호 통과~\n");
		}
		else {
			printf("\n입력한 비밀번호: ");

			for (i = 0; i < 4; i++)
				(input[i]);

			printf(" 가 틀렸음\n");
		}
}